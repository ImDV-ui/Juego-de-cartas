export class UIView {
    constructor() {
        // UI Elements like score, buttons, etc.
    }

    render() {
        // Update UI elements
    }

    updateScore(score) {
        // Update score display
    }
}
